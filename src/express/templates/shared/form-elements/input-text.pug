mixin input-text(type, name, id, value, className, label, isRequired)
  input.js-field(type=type class=className id = id name = name value=value required=isRequired)
  label(for=id)=label
  if isRequired
    span Обязательное поле
